Averigüe si una cadena contiene una secuencia de caracteres:

String myStr = "Hello";
System.out.println(myStr.contains("Hel"));   // true
System.out.println(myStr.contains("e"));     // true
System.out.println(myStr.contains("Hi"));    // false

Definición y uso
El contains()método comprueba si una cadena contiene una secuencia de caracteres.
Devuelve truesi los personajes existen y falsesi no.

Sintaxis
public boolean contains(CharSequence chars)

Valores paramétricos
Parameter	        Description
CharSequence chars	The characters to be searched for

La interfaz CharSequence es una secuencia legible de valores char, que se encuentra en el paquete java.lang.

Detalles técnicos
Devoluciones:	A boolean, que indica si existe una secuencia de caracteres en la cadena especificada:
                true - existe una secuencia de caracteres
                false - la secuencia de caracteres no existe
Lanza:	        NullPointerException- si el valor devuelto es nulo
Versión Java:	1.5